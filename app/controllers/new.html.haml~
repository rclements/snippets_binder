= error_messages_for(:snippet)

- form_for:snippet, @snippet, :url => { :action => "create" }, :html => { :multipart => true, :method => :put } do |form|
  = form.text_field :name
  = form.text_area :description
  = submit_tag 'Submit'
