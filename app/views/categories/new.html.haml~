= error_messages_for(:category)
= render :partial => "shared/side_nav"
.form-container
  - form_for :category, @category, :url => category_path do |form|
    .main-container
      %label{ :for => 'name'}
        Title
      = form.text_field :name
      %label{ :for => 'description'}
        Snippet
      = form.text_area :description, "class" => 'snippet-box'
    .submit-container
      = submit_tag 'Submit'
.clear
