= error_messages_for(:snippet)

- form_for :snippet, @snippet, :url => { :action => "update" }, :html => { :multipart => true, :method => :put } do |s|
  = s.text_field :name
  = s.text_area :description
  = submit_tag 'Edit'
